Place custom point brand logo PNG files here.

Expected names match the brand ids in `PointBrandCatalog`, for example:

- `korean_air.png`
- `marriott.png`
- `samsung_card.png`

When a file is missing, the app falls back to the existing bundled logo.
